import kotlin.test.assertEquals
import org.junit.jupiter.api.Test

class FirstTest {
    @Test
    fun test() {
        assertEquals(4, 2 * 2)
        assertEquals("Hello Sdimy!", "Hello" + " " + "Sdimy!")
    }
}